# PVQ1 protocol

この文書はPS1側の `dumper` とブラウザ側の `receiver` が共有する通信仕様です。
整数はすべてリトルエンディアン、座標は0始まりです。

## 画面

- 解像度: 320×240
- セル: 40列×30行
- 1セル: 8×8ピクセル
- 色: 8色、1セルあたり3ビット

色番号はRGBの各ビットを `R:G:B = bit2:bit1:bit0` として扱います。

| 値 | 色 |
|---:|---|
| 0 | 黒 |
| 1 | 青 |
| 2 | 緑 |
| 3 | シアン |
| 4 | 赤 |
| 5 | マゼンタ |
| 6 | 黄 |
| 7 | 白 |

外周1セルは黒です。5×5の位置マーカーを `(2,2)`、`(33,2)`、`(2,23)` に置きます。
8行目は `x mod 8`、8列目は `y mod 8` の色を表示します。この2本はタイミングパターンと、
録画ごとに変化する色・ホワイトバランスを補正するためのパレットを兼ねます。

外周、位置マーカー、8行目、8列目以外を上から行単位に走査すると924セルあります。
各セルへ上位ビットから3ビットずつ格納し、合計2772ビットを利用します。

## パケット

1画面の固定長パケットは346バイト（2768ビット）です。残り4ビットは交互パターンで
埋めます。

| Offset | Size | 内容 |
|---:|---:|---|
| 0 | 4 | Magic `PVQ1` |
| 4 | 1 | Version `1` |
| 5 | 1 | Flags。bit 0が1ならLZSS圧縮 |
| 6 | 4 | Transfer ID |
| 10 | 2 | Chunk index |
| 12 | 2 | Chunk count |
| 14 | 4 | 展開後の元データ総バイト数 |
| 18 | 4 | 元データ全体のCRC32 |
| 22 | 2 | このパケットのpayload長 |
| 24 | 318 | Payload、末尾はゼロ埋め |
| 342 | 4 | Offset 0～341のCRC32 |

CRC32は多項式 `0xEDB88320`、初期値と最終XORは `0xFFFFFFFF` です。

パケット構築後、固定seed `0xC001D00D` のxorshift32を1バイトごとに進め、その下位8ビットを
XORします。これは誤り訂正ではなく、画面上の色分布を偏りにくくするためのホワイトニングです。

Transfer IDは次の式で求めます。

```text
CRC32(image) XOR image_size XOR 0x50565152
```

## 転送前圧縮

送信側は起動時に元データをLZSSで圧縮し、圧縮後の方が小さい場合だけFlagsのbit 0を立てて
圧縮ストリームを送ります。圧縮で小さくならない場合はFlagsを0にして元データをそのまま送るため、
圧縮によって画面数が増えることはありません。Chunk countと各Payload長は実際に送るストリームを
基準にし、Total sizeとImage CRC32は常に展開後の元データを表します。

LZSSは4 KiBの後方参照窓を使います。トークンを8個ずつまとめ、先頭の1バイトをフラグとします。
フラグの下位bitから順に、1なら後続1バイトがリテラル、0なら後続2バイトが一致参照です。

一致参照の2バイトを `low`, `high` とすると、距離と長さは次の式です。

```text
distance = (((high & 0x0f) << 8) | low) + 1
length   = (high >> 4) + 3
```

したがって距離は1～4096バイト、長さは3～18バイトです。受信側は全チャンクを集めてから
圧縮ストリームを展開し、展開長と元データ全体のCRC32を検証します。不正な後方参照、切れた
トークン、余分な入力、展開長の不一致はいずれも復元失敗として扱います。

## 送信と回復

PS1は40×30テクセルの4bppテクスチャ（600バイト）をVRAMへPIO転送し、GPUの
raw-texture四角形で320×240へ拡大して裏ページへ描画します。画面切り替え後は3 VSyncを
明示的に保持します。全チャンクを通番順で送った後は、青地に周回番号付きの
`LOOP nnn END`を60 VSync（NTSCで約1秒）表示してから、先頭へ戻って無限に繰り返します。
周回番号は001～999で循環し、終了画面はデータパケットではありません。
受信側はフレームCRC32が一致したチャンクだけを保存し、重複を捨てます。全通番が揃った時点で
結合し、必要ならLZSS展開してから、最後に元データ全体のCRC32を検証します。

通信は一方向ですが、読み落としたチャンクを次の周回で回収できるため再送要求は不要です。

## 手ブレ補正

受信側は手動指定した四隅を初期位置とし、3個の5×5位置マーカーを各動画フレームの周辺から
探索します。マーカーの白黒パターンを輝度相関で評価し、3点の移動量からアフィン補正を求めて、
そのフレームの四隅を更新します。これにより通常の手持ち撮影で生じる平行移動、回転、緩やかな
拡大縮小とせん断を吸収します。

マーカーのコントラストまたは一致度が閾値を下回ったフレームは読み込まず、連続して見失った
場合は最大4セルまで探索範囲を広げて再捕捉します。激しいモーションブラー、画面外への移動、
ローリングシャッターによる大きな行単位の変形は補正対象外ですが、CRC棄却と次周回で回復できます。
